<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for KeyCare IME
    - Only allows HTTPS connections (cleartext disabled)
    - Trust system CA certificates
    - Ensures secure communication with API
-->
<network-security-config>
    <!-- Base config: Use HTTPS only -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    
    <!-- Allow Heroku API (HTTPS) -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">herokuapp.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
